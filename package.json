{
  "name": "dasar",
  "version": "1.0.0",
  "main": "index.ts",
  "license": "MIT",
  "scripts": {
    "tsc": "rm -rf build/ && tsc",
    "watch": "rm -rf build/ && tsc -w",
    "dev": "nodemon ./build/module/Product.js"
  },
  "devDependencies": {
    "nodemon": "^2.0.6",
    "typescript": "^4.1.2"
  }
}
